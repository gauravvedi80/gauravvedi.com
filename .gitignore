# OS
.DS_Store
**/.DS_Store

# Tooling / build artifacts
node_modules/
.wrangler/
dist/
*.log

# Local-only: copyrighted exam material — never publish
/Modul 3.4/
/siri-samples/

# Local-only: user-loaded tracks for the Strøm deck — the deck reads files at
# runtime; never bundle/publish full songs (copyright). Only the CC0 one-shots ship.
/strom/samples/*.mp3
/strom/samples/*.m4a
/strom/samples/*.ogg
/strom/samples/*.flac

# Internal build notes
/prompt.md
/docs/
/.claude/

# Pre-restyle backup of the Danish app — canonical version lives at dansk/index.html
/dansk-coach.html

# OG share-image render source (kept local; the rendered og-image.png is committed)
/og-card.html

# local dev test runner (never deploy)
/strom-test.mjs
